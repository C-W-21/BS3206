{
    "properties": {
        "src": {
            "properties": {
                "lat": { "type": "float64" },
                "lon": { "type": "float64" }
            }
        },
        "dest": {
            "properties": {
                "lat": { "type": "float64" },
                "lon": { "type": "float64" }
            }
        }
    },
    "optionalProperties": {
        "mode": {
            "enum": ["drive", "light_truck", "medium_truck", "truck", "heavy_truck", "truck_dangerous_goods", "long_truck"]
        },
        "type": {
            "enum": ["balanced", "short", "less_maneuvers"]
        },
        "traffic": {
            "enum": ["free_flow", "aproximated"]
        }
    }
}